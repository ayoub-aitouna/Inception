FROM debian:bookworm

RUN apt update
# RUN apt install -y php-fpm   php-pdo php-pdo-mysql mysql-client curl
# RUN mkdir -p /srv/www
# RUN curl https://wordpress.org/latest.tar.gz | www-data tar zx -C /srv/www
